import styled from 'styled-components';

export const ButtonClear = styled.button`
  cursor: pointer;
  color: inherit;
  border: none;
  background: none;

  &:focus {
    outline: none;
  }
`;

export default {};
