import styled from 'styled-components/macro';

export const Button = styled.button`
  :focus { outline: none; }
  border: none;
  background: none;
  cursor: pointer;
`;

export default {
  Button,
};
